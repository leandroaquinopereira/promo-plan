export enum TastingStatusEnum {
  DRAFT = 'draft',
  ACTIVE = 'active',
  COMPLETED = 'completed',
  CANCELLED = 'cancelled',
  DELETED = 'deleted',
  TODO = 'todo',
  IN_PROGRESS = 'in_progress',
  PAUSED = 'paused',
}
